# Heroku
export PATH="/usr/local/heroku/bin:$PATH"

# RVM
PATH="$PATH:$HOME/.rvm/bin"

# personnal bin in /opt
myBin=$('ls' -d /opt/**/bin | tr '\n' ':')
PATH="$PATH:$myBin"

# sbt
PATH="$PATH:/opt/sbt:/opt/play"
